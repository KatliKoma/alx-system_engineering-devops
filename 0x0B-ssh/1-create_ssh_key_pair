#!/usr/bin/env bash

# This script creates an RSA key pair with the specified requirements.

# Generate an RSA key pair with 4096 bits and protect it with the passphrase "betty"
ssh-keygen -t rsa -b 4096 -C "Generated by ALX script" -f ~/.ssh/school -N "betty"

echo "RSA key pair created successfully."
