# This SSH client configuration file sets up options for connecting to remote servers.

Host *
    # Use the private key ~/.ssh/school for authentication
    IdentityFile ~/.ssh/school
    
    # Refuse to authenticate using a password
    PasswordAuthentication no
